{
  "name": "ethicaletix",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "stage": "VITE_APP_ENV=stage vite",
    "prod": "VITE_APP_ENV=prod vite",
    "preview": "vite preview",
    "build": "tsc && vite build",
    "build-dev": "VITE_APP_ENV=dev tsc && vite build",
    "build-stage": "VITE_APP_ENV=stage tsc && vite build",
    "build-prod": "VITE_APP_ENV=prod tsc && vite build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-redux": "^8.0.5"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "@vitejs/plugin-react": "^3.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  }
}
